name: 04 - Using Actions

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    # checkout the code 
    # Install deps of our react application
    # execute automated tests
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Printing folders
        run: ls
